# ===== Database (MySQL) =====
spring.datasource.url=jdbc:mysql://localhost:3306/artworkdb?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===== JPA (Hibernate) =====
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ===== H2 console (not used) =====
spring.h2.console.enabled=false

# (Optional) External judge API
# If you had this before, keep it; otherwise leave it commented.
# external.judge.api.url=https://real-judge-api.example
